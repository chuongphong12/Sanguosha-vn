const fs = require("fs"); let f = "node_modules/boardgame.io/dist/boardgameio.js"; let p = fs.readFileSync(f, "utf8"); p = p.replace("const filteredInitialState = applyPlayerView(game, playerID, syncInfo.initialState);", "const filteredInitialState = applyPlayerView(game, playerID, syncInfo.initialState);\n            console.log(`FILTERING INITIAL STATE FOR ${playerID}`);"); fs.writeFileSync(f, p); let f2 = "node_modules/boardgame.io/dist/boardgameio.es.js"; let p2 = fs.readFileSync(f2, "utf8"); p2 = p2.replace("const filteredInitialState = applyPlayerView(game, playerID, syncInfo.initialState);", "const filteredInitialState = applyPlayerView(game, playerID, syncInfo.initialState);\n            console.log(`FILTERING INITIAL STATE FOR ${playerID}`);"); fs.writeFileSync(f2, p2);
