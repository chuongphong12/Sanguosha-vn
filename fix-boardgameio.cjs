const fs = require("fs"); let f = "node_modules/boardgame.io/dist/boardgameio.js"; let p = fs.readFileSync(f, "utf8"); p = p.replace("const filteredState = applyPlayerView(game, playerID, syncInfo.state);", "const filteredState = applyPlayerView(game, playerID, syncInfo.state);\n            const filteredInitialState = applyPlayerView(game, playerID, syncInfo.initialState);"); p = p.replace("state: filteredState,", "initialState: filteredInitialState,\n                state: filteredState,"); fs.writeFileSync(f, p); let f2 = "node_modules/boardgame.io/dist/boardgameio.es.js"; let p2 = fs.readFileSync(f2, "utf8"); p2 = p2.replace("const filteredState = applyPlayerView(game, playerID, syncInfo.state);", "const filteredState = applyPlayerView(game, playerID, syncInfo.state);\n            const filteredInitialState = applyPlayerView(game, playerID, syncInfo.initialState);"); p2 = p2.replace("state: filteredState,", "initialState: filteredInitialState,\n                state: filteredState,"); fs.writeFileSync(f2, p2);
