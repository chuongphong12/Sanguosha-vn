const fs = require("fs"); let p = fs.readFileSync("patches/boardgame.io@0.50.2.patch", "utf8"); p = p.replace(/(\+            const filteredInitialState = applyPlayerView\(game, playerID, syncInfo\.initialState\);\s+const log = redactLog\(syncInfo\.log, playerID\);\s+const newSyncInfo = \{\s+\.\.\.syncInfo,)/g, "$1\n+                initialState: filteredInitialState,"); fs.writeFileSync("patches/boardgame.io@0.50.2.patch", p);
