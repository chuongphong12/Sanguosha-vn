const fs = require("fs"); let f = "node_modules/boardgame.io/dist/boardgameio.js"; let p = fs.readFileSync(f, "utf8"); p = p.replace("switch (payload.type) {", "console.log(`getFilterPlayerView CALLED WITH TYPE: ${payload.type}`); switch (payload.type) {"); fs.writeFileSync(f, p); let f2 = "node_modules/boardgame.io/dist/boardgameio.es.js"; let p2 = fs.readFileSync(f2, "utf8"); p2 = p2.replace("switch (payload.type) {", "console.log(`getFilterPlayerView CALLED WITH TYPE: ${payload.type}`); switch (payload.type) {"); fs.writeFileSync(f2, p2);
