const fs = require("fs"); let p = fs.readFileSync("patches/boardgame.io@0.50.2.patch", "utf8"); p = p.replace(/\+                initialState: filteredInitialState,/g, "+                initialState: filteredInitialState,\n                 state: filteredState,\n                 log,\n             };\n             payload.args = [matchID, newSyncInfo];"); fs.writeFileSync("patches/boardgame.io@0.50.2.patch", p);
